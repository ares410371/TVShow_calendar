<%= form_for(tv_show_season) do |f| %>
  <% if tv_show_season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tv_show_season.errors.count, "error") %> prohibited this tv_show_season from being saved:</h2>

      <ul>
      <% tv_show_season.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tv_show_internal %>
    <%= f.number_field :tv_show_internal %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :overview %>
    <%= f.text_area :overview %>
  </div>

  <div class="field">
    <%= f.label :season_number %>
    <%= f.number_field :season_number %>
  </div>

  <div class="field">
    <%= f.label :air_date %>
    <%= f.text_field :air_date %>
  </div>

  <div class="field">
    <%= f.label :poster_path %>
    <%= f.text_field :poster_path %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
